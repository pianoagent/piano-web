---
/**
 * LogoWall — tenký wrapper nad @piano/ui/LogoWall.
 * Jen definuje septimí sadu log (public/logos) a výchozí label.
 * Vzhled a markup řeší sdílená komponenta — NEFORKOVAT.
 */
import UiLogoWall from '@piano/ui/components/LogoWall.astro';

interface Props { label?: string; background?: 'default' | 'alt'; id?: string; }
const { label = 'Důvěřují nám michelinské restaurace, hotely i kavárny za rohem', background = 'alt', id } = Astro.props;

const logos = [
  'ref-ambiente', 'ref-field-michelin-restaurant', 'ref-ladegustation-michelin', 'ref-entree-michelin',
  'ref-aromi-michelin-restaurant', 'ref-kolkovna', 'ref-sasazu', 'ref-zatisi-group',
  'ref-cafe-savoy', 'ref-pizza-coloseum', 'ref-420-restaurant', 'ref-02-arena',
  'ref-clarion-hotel', 'ref-grandhotel-pupp', 'ref-masarycka', 'ref-potrefena-husa',
  'ref-100-chuti-brna', 'ref-ugo',
];
---

<UiLogoWall logos={logos} label={label} background={background} id={id} />
