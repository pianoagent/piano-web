---
/**
 * Savarin — Demoverze. Krátká hero výzva + rovnou formulář žádosti (na přání majitele).
 * Sdílené komponenty @piano/ui.
 */
import Base from '../layouts/Base.astro';
import Header from '@piano/ui/components/Header.astro';
import Footer from '@piano/ui/components/Footer.astro';
import Breadcrumb from '@piano/ui/components/Breadcrumb.astro';
import Hero from '@piano/ui/components/Hero.astro';
import Heading from '@piano/ui/components/Heading.astro';
import LeadForm from '@piano/ui/components/LeadForm.astro';
import { savarinNav, savarinCta } from '../config/nav';

const schema = {
  '@context': 'https://schema.org',
  '@graph': [
    { '@type': 'BreadcrumbList', itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Domů', item: 'https://savarin.cz/' },
      { '@type': 'ListItem', position: 2, name: 'Demoverze', item: 'https://savarin.cz/demoverze' },
    ] },
  ],
};
---

<Base title="Demoverze Savarin zdarma — pokladna, sklady, recepce"
      description="Vyžádejte si bezplatnou demoverzi Savarin (pokladna, sklady a kalkulace, recepce). Vyplňte údaje a demoverzi vám zašleme e-mailem."
      schema={schema}>
  <Header brand="Savarin" logoSrc="/logos/savarin_logo.svg" logoHeight="36px" homeHref="/" nav={savarinNav} cta={savarinCta} />

  <main>

  <Breadcrumb items={[{ label: 'Domů', href: '/' }, { label: 'Demoverze' }]} />

  <Hero eyebrow="Demoverze zdarma" align="center" background="gradient" space="sm"
        lead="Vyplňte prosím své údaje — demoverzi (pokladna, sklady a kalkulace, recepce) vám zašleme e-mailem.">
    <Heading slot="title" as="h1" size="h1" align="center" color="on-dark">Žádost o zaslání demoverze Savarin</Heading>
  </Hero>

  <LeadForm id="poptavka" variant="full" layout="stacked" background="alt"
            message="Poznámka" messagePlaceholder="Poznámka, info o provozovně, požadavky…"
            submitLabel="Odeslat žádost"
            proofs={[]} successUrl="/dekujeme" />

  </main>

  <Footer legal={[{ label: 'Ochrana osobních údajů', href: '/ochrana-udaju' }]} logoSrc="/logos/savarin_by_piano.svg" logoAlt="Savarin by Piano" parentNote={{ text: 'Savarin je součástí skupiny Piano', href: 'https://piano.cz' }} />
</Base>
