---
/**
 * Savarin — Demoverze. Krátká hero výzva + přehled obsahu dema + formulář žádosti.
 * Sdílené komponenty @piano/ui.
 */
import Base from '../layouts/Base.astro';
import Header from '@piano/ui/components/Header.astro';
import Footer from '@piano/ui/components/Footer.astro';
import Breadcrumb from '@piano/ui/components/Breadcrumb.astro';
import Section from '@piano/ui/components/Section.astro';
import Hero from '@piano/ui/components/Hero.astro';
import Heading from '@piano/ui/components/Heading.astro';
import Text from '@piano/ui/components/Text.astro';
import Icon from '@piano/ui/components/Icon.astro';
import LeadForm from '@piano/ui/components/LeadForm.astro';
import { savarinNav, savarinCta } from '../config/nav';

const obsah = [
  { icon: 'lucide:monitor', title: 'Registrační pokladna', text: 'Demoverze pokladny Savarin. Po spuštění je na klávese F1 dostupný manuál.' },
  { icon: 'lucide:calculator', title: 'Sklady a kalkulace', text: 'Obsahuje programy HRMTZ a HRKALKUL — skladová evidence a kalkulace jídel.' },
  { icon: 'lucide:bed-double', title: 'Recepce', text: 'Demoverze modulu Recepce. Po spuštění je na klávese F1 dostupný manuál.' },
];

const schema = {
  '@context': 'https://schema.org',
  '@graph': [
    { '@type': 'BreadcrumbList', itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Domů', item: 'https://savarin.cz/' },
      { '@type': 'ListItem', position: 2, name: 'Demoverze', item: 'https://savarin.cz/demoverze' },
    ] },
  ],
};
---

<Base title="Demoverze Savarin zdarma — pokladna, sklady, recepce"
      description="Vyžádejte si bezplatnou demoverzi Savarin (pokladna, sklady a kalkulace, recepce). Vyplňte údaje a demoverzi vám zašleme e-mailem."
      schema={schema}>
  <Header brand="Savarin" logoSrc="/logos/savarin_logo.svg" logoHeight="36px" homeHref="/" nav={savarinNav} cta={savarinCta} />

  <main>

  <Breadcrumb items={[{ label: 'Domů', href: '/' }, { label: 'Demoverze' }]} />

  <Hero eyebrow="Demoverze zdarma" align="center" background="gradient" space="sm"
        lead="Vyzkoušejte Savarin nezávazně — pokladnu, sklady a kalkulace i recepci.">
    <Heading slot="title" as="h1" size="h1" align="center" color="on-dark">Demoverze Savarin</Heading>
  </Hero>

  <!-- CO DEMOVERZE OBSAHUJE -->
  <Section background="default" space="md">
    <div class="head head--center">
      <Heading as="h2" size="h2" align="center">Co demoverze obsahuje</Heading>
    </div>
    <div class="cards">
      {obsah.map((o) => (
        <article class="card">
          <span class="card__icon"><Icon name={o.icon} size="md" /></span>
          <Heading as="h3" size="h3">{o.title}</Heading>
          <Text size="small" color="muted">{o.text}</Text>
        </article>
      ))}
    </div>
    <Text size="small" color="muted" align="center" class="note">Ke každé demoverzi přikládáme stručný manuál. Zašleme ji elektronicky nebo na CD-ROM.</Text>
  </Section>

  <LeadForm id="poptavka" variant="full" background="alt"
            title="Žádost o demoverzi"
            lead="Vyplňte prosím své údaje — demoverzi vám zašleme e-mailem."
            message="Poznámka" messagePlaceholder="Poznámka, info o provozovně, požadavky…"
            submitLabel="Odeslat žádost"
            successUrl="/dekujeme" />

  </main>

  <Footer legal={[{ label: 'Ochrana osobních údajů', href: '/ochrana-udaju' }]} logoSrc="/logos/savarin_by_piano.svg" logoAlt="Savarin by Piano" parentNote={{ text: 'Savarin je součástí skupiny Piano', href: 'https://piano.cz' }} />
</Base>

<style>
  .head { max-width: 760px; margin-inline: auto; margin-bottom: var(--space-6); }
  .head--center { text-align: center; }

  .cards { display: grid; gap: var(--space-5); grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); }
  .card { background: var(--color-bg); border: 1px solid var(--color-border); border-radius: var(--radius-md); padding: var(--space-6) var(--space-5); transition: var(--transition); }
  .card:hover { border-color: var(--color-primary); box-shadow: var(--shadow-sm); }
  .card > * + * { margin-top: var(--space-3); }
  .card__icon { display: inline-flex; color: var(--color-text-accent); }
  .note { margin-top: var(--space-6); }
</style>
