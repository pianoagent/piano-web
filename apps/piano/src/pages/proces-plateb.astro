---
/**
 * Proces plateb — piano.cz/proces-plateb (vzhled dle reálné piano.cz).
 * Celá sekce na warm white, střídané kroky (text/obrázek), CTA + kontaktní pruh.
 */
import Base from '../layouts/Base.astro';
import Section from '@piano/ui/components/Section.astro';
import Heading from '@piano/ui/components/Heading.astro';
import Text from '@piano/ui/components/Text.astro';
import LeadForm from '@piano/ui/components/LeadForm.astro';

const steps = [
  { img: '/images/proces-1.webp', label: 'Udělení přístupu', title: 'Schválení bez účasti majitele/provozního', points: [
    'Majitelé podniků udělují personálu oprávnění autorizovat platby dodavatelům v Qerko administraci.',
    'Vaše údaje jsou v bezpečí, personál nikdy nevidí vaši platební kartu ani bankovní účet a nemá k nim přístup.',
  ] },
  { img: '/images/proces-2.webp', label: 'QR kód při převzetí zboží', title: 'Naskenování QR kódu při převzetí', points: [
    'Dodavatel při předání zboží vygeneruje unikátní QR kód propojený s konkrétní platbou a obchodníkem.',
    'Po obdržení zboží personál naskenuje QR kód pomocí aplikace Qerko.',
  ] },
  { img: '/images/proces-3.webp', label: 'Autorizace platby', title: 'Rychlé zaplacení', points: [
    'Pokud je platba autorizována (personálem podniku), objeví se jednoduchá obrazovka s možností „Autorizovat (částka)".',
  ] },
  { img: '/images/proces-4.webp', label: 'Ověření platby', title: 'Stav platby', points: [
    'Řidič rozvozu má ve svém zařízení okamžitou informaci, že platba byla úspěšně autorizována.',
  ] },
];
---

<Base title="Proces plateb dodavatelům, Piano Terminál"
      description="Jak funguje autorizace bezhotovostních plateb dodavatelům přes Piano Terminál a Qerko, krok za krokem, bezpečně a bez přístupu personálu k vašemu účtu.">

  <!-- Proces — celé na warm white, střídané kroky -->
  <Section background="warm" space="lg">
    <Heading as="h1" size="h1" align="center" class="pp-title">Proces autorizace plateb dodavatelům</Heading>
    <ol class="pp-steps">
      {steps.map((s) => (
        <li class="pp-step">
          <figure class="pp-step__img"><img src={s.img} alt={s.title} width="360" height="354" loading="lazy" /></figure>
          <div class="pp-step__body">
            <Text size="label" color="accent">{s.label}</Text>
            <Heading as="h2" size="h3">{s.title}</Heading>
            <ul class="pp-step__list">{s.points.map((p) => <li>{p}</li>)}</ul>
          </div>
        </li>
      ))}
    </ol>
  </Section>

  <!-- CTA -->
  <Section background="default" space="lg">
    <div class="pp-cta">
      <Heading as="h2" size="h2">Využívejte naše nástroje a posuňte své podnikání <em>na vyšší úroveň</em></Heading>
      <Text color="muted">Díky vzájemné spolupráci máme možnost přinášet na trh inovace a služby, které vám opravdu ušetří čas, zvýší zisk a zjednoduší život. Ozvěte se nám, uděláme vám nabídku šitou na míru vašim potřebám.</Text>
    </div>
  </Section>

  <!-- Kontaktní pruh -->
  <div class="contactband">
    <a class="contactband__half" href="tel:+420770320248">+420 770 320 248</a>
    <a class="contactband__half contactband__half--alt" href="mailto:info@piano.cz">info@piano.cz</a>
  </div>

  <LeadForm id="poptavka" variant="full"
            title="Kontaktujte nás ještě dnes pro konzultaci zdarma"
            lead="Ozvěte se nám a uděláme vám nabídku šitou na míru vašemu provozu." />

</Base>

<style>
  .pp-title { margin-bottom: var(--space-8); }
  .pp-steps { list-style: none; display: flex; flex-direction: column; gap: var(--space-8); max-width: 980px; margin: 0 auto; }
  .pp-step { display: grid; grid-template-columns: 0.9fr 1.1fr; gap: var(--space-7); align-items: center; }
  .pp-step:nth-child(even) .pp-step__img { order: 2; }
  .pp-step__img { margin: 0; display: grid; place-items: center; background: color-mix(in srgb, var(--color-secondary-terracotta) 14%, #FFF5E8); border-radius: var(--radius-md); padding: var(--space-6); }
  .pp-step__img img { display: block; width: 100%; max-width: 260px; height: auto; }
  .pp-step__body :global(h2) { margin: var(--space-1) 0 var(--space-3); }
  .pp-step__list { list-style: none; display: flex; flex-direction: column; gap: var(--space-3); }
  .pp-step__list li { position: relative; padding-left: var(--space-5); color: var(--color-text-muted); }
  .pp-step__list li::before { content: "•"; position: absolute; left: var(--space-1); color: var(--color-text-accent); font-weight: 700; }

  /* CTA */
  .pp-cta { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-7); align-items: center; max-width: var(--container-max); }

  /* Kontaktní pruh — dvě karmínové poloviny */
  .contactband { display: flex; flex-wrap: wrap; }
  .contactband__half { flex: 1 1 50%; text-align: center; text-decoration: none; color: var(--color-text-on-dark); background: var(--color-primary-bg); padding: var(--space-6) var(--space-4); font-size: var(--font-size-lead); font-weight: var(--font-weight-medium); transition: var(--transition); }
  .contactband__half:hover { background: var(--color-primary-bg-hover); }
  .contactband__half--alt { background: var(--color-primary); }
  .contactband__half--alt:hover { background: var(--color-primary-hover); }

  @media (max-width: 720px) {
    .pp-step { grid-template-columns: 1fr; gap: var(--space-4); }
    .pp-step:nth-child(even) .pp-step__img { order: 0; }
    .pp-cta { grid-template-columns: 1fr; gap: var(--space-4); }
    .contactband__half { flex-basis: 100%; }
  }
</style>
